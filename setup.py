from setuptools import setup, find_packages

setup(name='outlier-detection',
      version='0.0.1',
      description='What it does',
      author='',
      author_email='',
      url='',
      packages=find_packages(),
      )
