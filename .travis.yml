  language: python
  dist: xenial   # required for Python >= 3.7
  python:
    - "3.7"
  before_install:
    - "pip install -U pip"
    - "python setup.py install"
  install:
    - pip install -r requirements.txt
  script: python Test/TestLOF.py


